// User-applied corrections (the edits the user has made and that get
// saved with the project). Exposed as a global so app.js can read it
// without a fetch() call.
window.SCML_CORRECTIONS = JSON.parse("{}");
